<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_helpdesk_ticket_tree" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.tree</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <tree string="Ticket">
                <field name="name" />
                <field name="description" />
                <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                <field name="state" />
                <field name="date" />
            </tree>
        </field>
    </record>

    <record id="view_helpdesk_ticket_form" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.form</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <form string="Ticket">
                <header>
                    <button string="Assigned" name="mark_assigned" type="object" attrs="{'invisible':[('state','!=','new')]}" />
                    <button string="In process" name="mark_inprocess" type="object" states="assigned" />
                    <button string="Pending" name="mark_pending" type="object" states="inprocess" />
                    <button string="Solved" name="mark_solved" type="object" states="pending" />
                    <button string="Cancel" name="mark_canceled" type="object" attrs="{'invisible':[('state','==','canceled')]}" />
                    <button string="Mark as new" name="set_reset" type="object" attrs="{'invisible':[('state','==','new')]}" />
                    <field name="state" widget="statusbar" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>
                    <group>
                        <group name="main">
                            <field name="date" />
                            <field name="date_limit" />
                        </group>
                        <group name="main">
                            <field name="user_id" widget="many2one_avatar_user"/>
                            <field name="assigned" />
                            <field name="ticket_qty" />
                            
                            <field name="time"  widget="float_time"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" />
                            <group>
                                <field name="tag_name"/>
                                <button string="Create tag"
                                name="create_tag"
                                type="object"/>
                            </group>
                            <group name="Tags">
                                <field name="tag_ids" string='Tags'/>
                            </group>
                        </page>
                        <page string="Action Corrective">
                            <field name="action_corrective" />
                        </page>
                        <page string="Action Preventive">
                            <field name="action_preventive" />
                        </page>
                        <page string="Actions">
                            <field name="action_ids" />
                        </page>
                    </notebook>
                    
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_helpdesk_ticket_search" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.search</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <search string="Ticket">
                <field name="name" />
                <field name="user_id" />

                <filter string="Assigned" name="assigned_filter" domain="[('user_id', '!=', False)]"/>
                <filter string="No date limit" name="date_limit_filter" domain="[('date_limit', '!=', False)]"/>

                <filter name="user_id_groupby" string="date_limit" context="{'group_by': 'date_limit'}" />
                <filter name="user_id_groupby" string="user_id" context="{'group_by': 'user_id'}" />
            </search>
        </field>
    </record>

    <record id="view_helpdesk_ticket_pivot" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.pivot</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <pivot string="Ticket">
                <field name="user_id" type="row"/>
                <field name="date" interval="month" type="col"/>
                <field name="time" type="measure" widget="float_time" />
            </pivot>
        </field>
    </record>

    <record id="view_helpdesk_ticket_graph" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.graph</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <graph string="Ticket_User">
                <field name="user_id" />
                <field name="time" type="measure" widget="float_time" />
            </graph>
        </field>
    </record>

    <record id="view_helpdesk_ticket_calendar" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.calendar</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <calendar string="Tickets" date_start="date" color="user_id" hide_time="true" event_limit="5">
                <field name="user_id" avatar_field="image_128" />
                <field name="name" />
            </calendar>
        </field>
    </record>

    <record id="view_helpdesk_ticket_kanban" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.kanban</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <kanban default_group_by="user_id">
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_content oe_kanban_global_click">
                            <div class="o_kanban_record_title">
                                <b><field name="name" /></b>
                            </div>
                            <div class="o_kanban_record_subtitle">

                                <field name="description" />
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_right">
                                    <field name="user_id" widget="many2one_avatar_user"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>


    <record id="helpdesk_ticket_action" model="ir.actions.act_window">
        <field name="name">Ticket</field>
        <field name="res_model">helpdesk.ticket</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to create a new Ticket.
            </p>
        </field>
    </record>

    <menuitem id="helpdesk_ticket_menu" parent=""
        action="helpdesk_ticket_action" sequence="20"/>
</odoo>